---
metaTitle: Switch
metaDescription: Use as an alternative to the checkbox component.
---

```jsx live=true
<Switch />
```

## API Reference

This component inherits the props from the [Radix Switch](/docs/primitives/components/switch) primitive and supports common margin props `m`, `mx`, `my`, `ml`, `mr`, `mt`, `mb`.

<ThemesPropsTable
  data={(variables) => [
    {
      name: 'size',
      required: false,
      type: variables.switch.size.values,
      default: variables.switch.size.default,
      typeSimple: 'enum',
    },
    {
      name: 'variant',
      required: false,
      type: variables.switch.variant.values,
      default: variables.switch.variant.default,
      typeSimple: 'enum',
    },
    {
      name: 'color',
      required: false,
      typeSimple: 'enum',
      default: variables.switch.color.default,
      description: (
        <>
          Use to override the default color inherited from the global theme.
          Read our <a href="/TODO">theming guide</a> for more details.
        </>
      ),
    },
    {
      name: 'radius',
      required: false,
      typeSimple: 'enum',
      default: variables.switch.radius.default,
      description: (
        <>
          Use to override the default radius inherited from the global theme.
          Read our <a href="/TODO">theming guide</a> for more details.
        </>
      ),
    },
  ]}
/>

## Examples

### Variant

Use the `variant` prop to control the visual color variation.

```jsx live=true
<Flex gap="3">
  <Switch variant="solid" defaultChecked />
  <Switch variant="solid-mono" defaultChecked />
</Flex>
```

### Size

Use the `size` prop to control the size.

```jsx live=true
<Flex gap="3">
  <Switch size="1" />
  <Switch size="2" />
  <Switch size="3" />
</Flex>
```
