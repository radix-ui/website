---
metaTitle: Checkbox
metaDescription: Use to select one or more items from a set of values.
---

```jsx live=true
<Checkbox />
```

## API Reference

This component inherits the props from the [Radix Checkbox](/docs/primitives/components/checkbox) primitive and supports common margin props `m`, `mx`, `my`, `ml`, `mr`, `mt`, `mb`.

<ThemesPropsTable
  data={(variables) => [
    {
      name: 'size',
      required: false,
      type: variables.checkbox.size.values,
      default: variables.checkbox.size.default,
      typeSimple: 'enum',
    },
    {
      name: 'variant',
      required: false,
      type: variables.checkbox.variant.values,
      default: variables.checkbox.variant.default,
      typeSimple: 'enum',
    },
    {
      name: 'color',
      required: false,
      typeSimple: 'enum',
      default: variables.checkbox.color.default,
      description: <>Use to override the default color inherited from the global theme. Read our <a href="/TODO">theming guide</a> for more details.</>,
    },
    {
      name: 'radius',
      required: false,
      typeSimple: 'enum',
      default: variables.checkbox.radius.default,
      description: <>Use to override the default radius inherited from the global theme. Read our <a href="/TODO">theming guide</a> for more details.</>,
    },

]}
/>

## Examples

### Variant

Use the `variant` prop to control the visual style.

```jsx live=true
<Flex direction="column" gap="2">
  <label>
    <Flex gap="2" align="center">
      <Checkbox variant="solid" defaultChecked />
      <Text>Agree to Terms and Conditions</Text>
    </Flex>
  </label>

  <label>
    <Flex gap="2" align="center">
      <Checkbox variant="solid-mono" defaultChecked />
      <Text>Agree to Terms and Conditions</Text>
    </Flex>
  </label>
</Flex>
```

### Size

Use the `size` prop to control the size.

```jsx live=true
<Flex gap="3" align="center">
  <Checkbox size="1" defaultChecked />
  <Checkbox size="2" defaultChecked />
</Flex>
```
