---
metaTitle: Text Area
metaDescription: Use to capture multi-line user input via text area.
---

```jsx live=true
<TextArea placeholder="The quick brown fox jumped" />
```

## API Reference

This component is based on the `textarea` element and supports common margin props `m`, `mx`, `my`, `ml`, `mr`, `mt`, `mb`.

<ThemesPropsTable
  data={(variables) => [
    {
      name: 'size',
      required: false,
      type: variables.textArea.size.values,
      default: variables.textArea.size.default,
      typeSimple: 'enum',
    },
    {
      name: 'variant',
      required: false,
      type: variables.textArea.variant.values,
      default: variables.textArea.variant.default,
      typeSimple: 'enum',
    },
    {
      name: 'radius',
      required: false,
      typeSimple: 'enum',
      default: variables.textArea.radius.default,
      description: (
        <>
          Use to override the default radius inherited from the global theme.
          Read our <a href="/TODO">theming guide</a> for more details.
        </>
      ),
    },
  ]}
/>

## Examples

### Variant

Use the `variant` prop to control the visual color variation.

```jsx live=true
<Grid columns="3" gap="3">
  <TextArea variant="surface" placeholder="The quick brown fox jumped" />
  <TextArea variant="surface-mono" placeholder="The quick brown fox jumped" />
  <TextArea variant="subtle-mono" placeholder="The quick brown fox jumped" />
</Grid>
```

### Size

Use the `size` prop to control the size.

```jsx live=true
<Grid gap="3">
  <TextArea size="1" placeholder="The quick brown fox jumped" />
  <TextArea size="2" placeholder="The quick brown fox jumped" />
  <TextArea size="3" placeholder="The quick brown fox jumped" />
</Grid>
```
