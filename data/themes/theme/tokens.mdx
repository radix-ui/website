---
metaTitle: Theme tokens
metaDescription:
  Tokens provide direct access to theme values and give you full flexibility to
  build and customize your own themed components.
---

# Theme tokens

<Description>
  Tokens provide direct access to theme values and give you full flexibility to
  build and customize your own themed components.
</Description>

## Using tokens

Radix Themes exposes all tokens as [CSS Custom Properties](https://developer.mozilla.org/en-US/docs/Web/CSS/--*), these are consumed internally in all styled components.

You can access them the same way you would any other variable, either directly inside your CSS or inline via the `style` prop.

### Modifying tokens

Token values can be customized by using a separate configuration stylesheet. The simplest way to do so is to create a `theme-config.css` file and ensure it is imported after the global theme styles:

```css
/* theme-config.css */
.radix-themes {
  --default-font-size: 18px;
}
```

```jsx
// app-root.jsx
import '@radix-ui/themes/dist/index.css';
import './themes-config.css';

import { Theme } from '@radix-ui/themes';

export default function () {
  return (
    <Theme>
      <MyApp />
    </Theme>
  );
}
```

### Layout scaling

Values which affect layout (spacing, typographic sizing, line heights) scale relatively based on the `scaling` value defined in your [Theme](/docs/themes/components/theme). This setting allows you to scale the UI density uniformly across your entire application.

The values illustrated below represent the "base" values (defaulting to `100%`).

## Color

Radix Themes uses [Radix Colors](/colors/docs/palette-composition/scales) as the foundation of it's color system. Out of the box this provides you with 30, 12 step color scales, each with it's own light and dark variant.

### Accent

Accents are the most dominant color in your theme, they are used for primary buttons, links and other interactive elements. The accent can be one of 23 scales specified by your [Theme](/docs/themes/components/theme).

<ThemesColorTable type="accent" />

<ThemesTokensDescriptionTable
  data={[
    {
      token: '--accent-{step}',
      description: 'The color for the given step',
    },
    {
      token: '--accent-a{step}',
      description: 'The alpha variant for the given step',
    },
  ]}
/>

### Gray

Gray colors are used for backgrounds, borders, text and other non-interactive elements. You can choose from one of 6 gray scales specified by your [Theme](/docs/themes/components/theme).

<ThemesColorTable type="gray" />

<ThemesTokensDescriptionTable
  data={[
    {
      token: '--gray-{step}',
      description: 'The color value for the given step',
    },
    {
      token: '--gray-a{step}',
      description: 'The alpha variant value for the given step',
    },
  ]}
/>

### Alpha variants

Every scale has an alpha variant (denoted by the `a` scale prefix). These variants are designed to appear visually the same as their opaque counterparts when placed over a matching background. This is very powerful and allows you to overlay colors without a loss of saturation or hue.

### Additional scales

While the Accent and Gray scales are the sets you'll primarily be using throughout your app, you can use any of the scales available in [Radix Colors](/colors/docs/palette-composition/scales):

<ThemesAdditionalColors />

This is useful if you need to assign semantic color, for example, positive, negative, or warning.

## Spacing

Spacing tokens are used to apply space between elements. They are derived from a 9 step scale.

<ThemesSpacingTable />

<ThemesTokenValuesTable
  data={[
    {
      token: '--space-1',
      value: '4px',
    },
    {
      token: '--space-2',
      value: '8px',
    },
    {
      token: '--space-3',
      value: '12px',
    },
    {
      token: '--space-4',
      value: '16px',
    },
    {
      token: '--space-5',
      value: '24px',
    },
    {
      token: '--space-6',
      value: '32px',
    },
    {
      token: '--space-7',
      value: '40px',
    },
    {
      token: '--space-8',
      value: '48px',
    },
    {
      token: '--space-9',
      value: '64px',
    },
  ]}
/>

## Visual style

These tokens represent the visual properties of an element. Such as border radius and shadow.

### Border radius

<ThemesBorderRadiusTable />

<ThemesTokenValuesTable
  data={[
    {
      token: '--radius-1',
      value: '3px',
    },
    {
      token: '--radius-2',
      value: '4px',
    },
    {
      token: '--radius-3',
      value: '6px',
    },
    {
      token: '--radius-4',
      value: '8px',
    },
    {
      token: '--radius-5',
      value: '12px',
    },
    {
      token: '--radius-6',
      value: '16px',
    },
  ]}
/>

### Shadow

<ThemesShadowTable />

<ThemesTokensDescriptionTable
  data={[
    {
      token: '--shadow-{step}',
      description: 'The shadow value for the given step',
    },
  ]}
/>

## Typography

The typographic system is based on a 9 step scale. The defaults are tuned to get the best out of the default font stack, though you can override these values to optimize for your own typeface of choice.

<ThemesFontSizeTable />

### Type scale

The type scale is at the core of the typographic system, every step has a corresponding `font-size`, `line-height` and `letter-spacing` value, they are designed to be used in combination with each other.

<ThemesTokensDescriptionTable
  data={[
    {
      token: '--font-size-{step}',
      description: 'The size value for the given step',
    },
    {
      token: '--line-height-{step}',
      description: 'The line height value for the given step',
    },
    {
      token: '--letter-spacing-{step}',
      description: 'The letter spacing value for the given step',
    },
  ]}
/>

Their base values and relationships are illustrated below:

<ThemesFontSizingTable
  data={[
    {
      step: 1,
      fontSize: '12px',
      letterSpacing: '0.0025em',
      lineHeight: '16px',
    },
    { step: 2, fontSize: '14px', letterSpacing: '0em', lineHeight: '20px' },
    { step: 3, fontSize: '16px', letterSpacing: '0em', lineHeight: '24px' },
    {
      step: 4,
      fontSize: '18px',
      letterSpacing: '-0.0025em',
      lineHeight: '26px',
    },
    {
      step: 5,
      fontSize: '20px',
      letterSpacing: '-0.005em',
      lineHeight: '28px',
    },
    {
      step: 6,
      fontSize: '24px',
      letterSpacing: '-0.00625em',
      lineHeight: '30px',
    },
    {
      step: 7,
      fontSize: '28px',
      letterSpacing: '-0.0075em',
      lineHeight: '36px',
    },
    { step: 8, fontSize: '35px', letterSpacing: '-0.01em', lineHeight: '40px' },
    {
      step: 9,
      fontSize: '60px',
      letterSpacing: '-0.025em',
      lineHeight: '60px',
    },
  ]}
/>

### Font weight

Font weight tokens are used to align named weights with those specified by your typeface.

<ThemesTokenValuesTable
  data={[
    {
      token: '--font-weight-light',
      value: '300',
    },
    {
      token: '--font-weight-regular',
      value: '400',
    },
    {
      token: '--font-weight-medium',
      value: '500',
    },
    {
      token: '--font-weight-bold',
      value: '700',
    },
  ]}
/>

### Defaults

These tokens represent the base typographic attributes of the theme.

<ThemesTokenValuesTable
  data={[
    {
      token: '--default-font-family',
      value:
        "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', 'Open Sans', system-ui, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji'",
    },
    {
      token: ' --default-font-size',
      value: 'var(--font-size-3)',
    },
    {
      token: '--default-font-style',
      value: 'normal',
    },
    {
      token: '--default-font-weight',
      value: 'var(--font-weight-regular)',
    },
    {
      token: '--default-line-height',
      value: '1.5',
    },
    {
      token: '--default-letter-spacing',
      value: '0em',
    },
    {
      token: '--default-leading-trim-start',
      value: '0.42em',
      description: (
        <>
          The amount of top offset trim applied when using the <Code>trim</Code>{' '}
          prop.
        </>
      ),
    },
    {
      token: '--default-leading-trim-end',
      value: '0.36em',
      description: (
        <>
          The amount of bottom offset trim applied when using the{' '}
          <Code>trim</Code> prop.
        </>
      ),
    },
  ]}
/>

### Advanced settings

In addition to the global defaults, you also have access to the typographic attributes for specific elements within a theme.

#### Heading

Typographic attributes for the [Heading](/docs/themes/components/heading) component.

<ThemesTokenValuesTable
  data={[
    {
      token: '--heading-font-family',
      value: 'var(--default-font-family)',
    },
    {
      token: '--heading-font-size-adjust',
      value: '1',
    },
    {
      token: '--heading-font-style',
      value: 'normal',
    },
    {
      token: '--heading-leading-trim-start',
      value: 'var(--default-leading-trim-start)',
      description: (
        <>
          The amount of top offset trim applied when using the <Code>trim</Code>{' '}
          prop.
        </>
      ),
    },
    {
      token: '--heading-leading-trim-end',
      value: 'var(--default-leading-trim-end)',
      description: (
        <>
          The amount of bottom offset trim applied when using the{' '}
          <Code>trim</Code> prop.
        </>
      ),
    },
    {
      token: '--heading-letter-spacing',
      value: '0em',
    },
  ]}
/>

Heading `line-height` can also be customized independently of the base settings:

<ThemesTokenValuesTable
  data={[
    {
      token: '--heading-line-height-1',
      value: '16px',
    },
    {
      token: '--heading-line-height-2',
      value: '18px',
    },
    {
      token: '--heading-line-height-3',
      value: '22px',
    },
    {
      token: '--heading-line-height-4',
      value: '24px',
    },
    {
      token: '--heading-line-height-5',
      value: '26px',
    },
    {
      token: '--heading-line-height-6',
      value: '30px',
    },
    {
      token: '--heading-line-height-7',
      value: '36px',
    },
    {
      token: '--heading-line-height-8',
      value: '40px',
    },
    {
      token: '--heading-line-height-9',
      value: '60px',
    },
  ]}
/>

### Code

Typographic attributes for the [Code](/docs/themes/components/code) component.

<ThemesTokenValuesTable
  data={[
    {
      token: '--code-font-family',
      value:
        "'Fira --Code', 'Menlo', monospace, 'Apple Color Emoji', 'Segoe UI Emoji'",
    },
    {
      token: '--code-font-size-adjust',
      value: '0.9',
    },
    {
      token: '--code-font-style',
      value: 'normal',
    },
    {
      token: '--code-font-weight',
      value: 'inherit',
    },
    {
      token: '--code-letter-spacing',
      value: '-0.007em',
    },
  ]}
/>

### Strong

Typographic attributes for the [Strong](/docs/themes/components/strong) component.

<ThemesTokenValuesTable
  data={[
    {
      token: '--strong-font-family',
      value: 'var(--default-font-family)',
    },
    {
      token: '--strong-font-size-adjust',
      value: '1',
    },
    {
      token: '--strong-font-style',
      value: 'inherit',
    },
    {
      token: '--strong-font-weight',
      value: 'var(--font-weight-bold)',
    },
    {
      token: '--strong-letter-spacing',
      value: '0em',
    },
  ]}
/>

### Em

Typographic attributes for the [Em](/docs/themes/components/em) component.

<ThemesTokenValuesTable
  data={[
    {
      token: '--em-font-family',
      value: 'var(--default-font-family)',
    },
    {
      token: '--em-font-size-adjust',
      value: '1',
    },
    {
      token: '--em-font-style',
      value: 'inherit',
    },
    {
      token: '--em-font-weight',
      value: 'var(--font-weight-bold)',
    },
    {
      token: '--em-letter-spacing',
      value: '0em',
    },
  ]}
/>
