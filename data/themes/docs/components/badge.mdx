---
metaTitle: Badge
metaDescription: A stylized badge element.
sourcePath: components/badge
---

```jsx live=true
<Flex gap="2">
  <Badge color="orange">In progress</Badge>
  <Badge color="blue">In review</Badge>
  <Badge color="green">Complete</Badge>
</Flex>
```

## API Reference

This component is based on the `span` element and supports [common margin props](/themes/docs/theme/layout#margin-props).

<ThemesPropsTable name="badge" />

## Examples

### Size

Use the `size` prop to control the size.

```jsx live=true
<Flex align="center" gap="2">
  <Badge size="2" color="indigo">
    Enhancement
  </Badge>

  <Badge size="1" color="indigo">
    Enhancement
  </Badge>
</Flex>
```

### Variant

Use the `variant` prop to control the visual style.

```jsx live=true scroll=true
<Flex gap="2">
  <Badge variant="solid" color="indigo">
    Enhancement
  </Badge>
  <Badge variant="soft" color="indigo">
    Enhancement
  </Badge>
  <Badge variant="outline" color="indigo">
    Enhancement
  </Badge>
  <Badge variant="surface" color="indigo">
    Enhancement
  </Badge>
</Flex>
```

### Color

Use the `color` prop to assign a specific [color](/themes/docs/theme/color), ignoring the global theme.

```jsx live=true
<Flex gap="2">
  <Badge color="indigo">Enhancement</Badge>
  <Badge color="cyan">Enhancement</Badge>
  <Badge color="orange">Enhancement</Badge>
  <Badge color="crimson">Enhancement</Badge>
</Flex>
```

### High-contrast

Use the `highContrast` prop to add additional contrast.

```jsx live=true
<Flex direction="column" gap="2">
  <Flex gap="2">
    <Badge variant="solid">Enhancement</Badge>
    <Badge variant="soft">Enhancement</Badge>
    <Badge variant="outline">Enhancement</Badge>
    <Badge variant="surface">Enhancement</Badge>
  </Flex>
  <Flex gap="2">
    <Badge variant="solid" highContrast>
      Enhancement
    </Badge>
    <Badge variant="soft" highContrast>
      Enhancement
    </Badge>
    <Badge variant="outline" highContrast>
      Enhancement
    </Badge>
    <Badge variant="surface" highContrast>
      Enhancement
    </Badge>
  </Flex>
</Flex>
```

### Radius

Use the `radius` prop to assign a specific radius value, ignoring the global theme.

```jsx live=true scroll=true
<Flex gap="2">
  <Badge variant="solid" radius="none" color="indigo">
    Enhancement
  </Badge>
  <Badge variant="solid" radius="large" color="indigo">
    Enhancement
  </Badge>
  <Badge variant="solid" radius="full" color="indigo">
    Enhancement
  </Badge>
</Flex>
```
