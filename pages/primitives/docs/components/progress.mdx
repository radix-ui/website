---
title: Progress
name: progress
version: 0.0.1-13
description: Displays an indicator showing the completion progress of a task, typically displayed as a progress bar.
aria: https://www.w3.org/TR/wai-aria-1.2/#progressbar
features:
  - Provides context for assistive technology to read the progress of a task.
---

import { ProgressHero } from '../../../../components/Hero/ProgressHero';

<HeroSlot>
  <ProgressHero />
</HeroSlot>

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-progress
```

### Anatomy

Import the components and piece the parts together.

```jsx
import { Progress, Indicator } from '@radix-ui/react-progress';

export default () => (
  <Progress>
    <Indicator />
  </Progress>
);
```

### Basic example

Create your styled progress component from the primitive parts.

**Note:** This example uses [Stitches](https://stitches.dev) but any styling solution is [compatible](../overview/styling).

```jsx live manual
// import { styled } from 'path-to/stitches.config';
import * as ProgressBarPrimitive from '@radix-ui/react-progress';

const Progress = styled(ProgressBarPrimitive.Root, {
  position: 'relative',
  height: 10,
  overflow: 'hidden',
  borderRadius: 5,
  background: 'gainsboro',
});

const ProgressIndicator = styled(ProgressBarPrimitive.Indicator, {
  boxSizing: 'border-box',
  position: 'absolute',
  backgroundColor: 'dodgerblue',
  height: '100%',
});

export () => (
  <Progress value={50}>
    <ProgressIndicator style={{ width: '50%' }} />
  </Progress>
);
```

### API Reference

#### Progress

`Progress` is the root component.

<PropsTable
  data={[
    {
      name: 'value',
      type: 'number | null',
      description: (
        <span>
          The value of the <Code>Progress</Code>
        </span>
      ),
    },
    {
      name: 'max',
      type: 'number',
      description: (
        <span>
          The maximum value the <Code>Progress</Code> can reach.
        </span>
      ),
    },
    {
      name: 'getValueLabel',
      type: '(value: number, max: number) => string',
      typeSimple: 'function',
      description:
        'A function to get the accessible label text representing the current value in a human-readable format. If not provided, the value label will be read as the numeric value as a percentage of the max value.',
    },
  ]}
/>

#### Indicator

Used to show the progress visually. It also makes progress accessible to assistive technologies.
