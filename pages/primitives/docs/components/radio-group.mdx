---
title: Radio Group
name: radio-group
version: 0.0.1-13
description: A set of checkable buttons—known as radio buttons—where no more than one of the buttons can be checked at a time.
features:
  - Full keyboard navigation.
  - Can be controlled or uncontrolled.
---

import { RadioHero } from '../../../../components/Hero/RadioHero';

<HeroSlot>
  <RadioHero />
</HeroSlot>

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-radio-group
```

Then import the component in your component file.

```jsx
// RadioGroup.tsx
import * as RadioGroupPrimitive from '@radix-ui/react-radio-group';
```

### Basic example

```jsx live manual
// RadioGroup.tsx
// import * as RadioGroupPrimitive from '@radix-ui/react-radio-group';

const Radio = styled(RadioGroupPrimitive.Item, {
  appearance: 'none',
  backgroundColor: 'transparent',
  border: 'none',
  padding: 0,
  borderRadius: '50%',
  boxShadow: 'inset 0 0 0 1px gainsboro',
  width: 15,
  height: 15,
  display: 'inline-flex',
  alignItems: 'center',
  justifyContent: 'center',
  verticalAlign: 'middle',

  '& ~ &': { marginLeft: 5 },

  '&:focus': {
    outline: 'none',
    boxShadow: 'inset 0 0 0 1px dodgerblue, 0 0 0 1px dodgerblue',
  },
});

const Indicator = styled(RadioGroupPrimitive.Indicator, {
  width: 7,
  height: 7,
  borderRadius: '50%',
  backgroundColor: 'dodgerblue',
});

render(
  <RadioGroupPrimitive.Root>
    <Radio value="cat">
      <Indicator />
    </Radio>
    <Radio value="dog">
      <Indicator />
    </Radio>
    <Radio value="rabbit">
      <Indicator />
    </Radio>
  </RadioGroupPrimitive.Root>
);
```

### Accessibility

#### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Tab'],
      description: (
        <span>
          Moves focus to either the checked <Code>Item</Code> or the first <Code>Item</Code> in the group.
        </span>
      ),
    },
    {
      keys: ['Space'],
      description: (
        <span>
          When focus is on an unchecked <Code>Item</Code>, checks the{' '}
          <Code>Item</Code>.
        </span>
      ),
    },
    {
      keys: ['Enter'],
      description: (
        <span>
          When focus is on an unchecked <Code>Item</Code>, checks the{' '}
          <Code>Item</Code>.
        </span>
      ),
    },
    {
      keys: ['ArrowDown'],
      description: (
        <span>
          Moves focus to the next <Code>Item</Code> in the group.
        </span>
      ),
    },
    {
      keys: ['ArrowRight'],
      description: (
        <span>
          Moves focus to the next <Code>Item</Code> in the group.
        </span>
      ),
    },
    {
      keys: ['ArrowUp'],
      description: (
        <span>
          Moves focus to the previous <Code>Item</Code> in the group.
        </span>
      ),
    },
    {
      keys: ['ArrowLeft'],
      description: (
        <span>
          Moves focus to the previous <Code>Item</Code> in the group.
        </span>
      ),
    },
  ]}
/>

### API Reference

#### RadioGroup

`RadioGroup` is the root component.

<PropsTable
  data={[
    {
      name: 'defaultValue',
      type: 'string',
      description: (
        <span>
          The value of the selected <Code>Item</Code> when the{' '}
          <Code>RadioGroup</Code> is initially rendered. Use{' '}
          <Code>defaultValue</Code> if you do not need to control the state of a{' '}
          <Code>RadioGroup</Code>.
        </span>
      ),
    },
    {
      name: 'value',
      type: 'string',
      description: (
        <span>
          The controlled value of the <Code>RadioGroup</Code>`. Must be used in
          conjunction with <Code>onValueChange</Code>.
        </span>
      ),
    },
    {
      name: 'onValueChange',
      type: '(value: string) => void',
      typeSimple: 'function',
      description: (
        <span>The callback that fires when the selected value changes.</span>
      ),
    },
    {
      name: 'required',
      type: 'boolean',
      description: (
        <span>Whether or not this choice is required in a form.</span>
      ),
    },
  ]}
/>

#### Item

A single item in a `RadioGroup`.

<PropsTable
  data={[
    {
      name: 'value',
      type: 'string',
      required: true,
      description: (
        <span>
          The value behind the <Code>Item</Code>.
        </span>
      ),
    },
    {
      name: 'onCheckedChange',
      type: '(event: React.ChangeEvent<HTMLInputElement>) => void',
      typeSimple: 'function',
      description: (
        <span>
          The callback that fires when the checked state of the{' '}
          <Code>Item</Code> changes.
        </span>
      ),
    },
    {
      name: 'disabled',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Item</Code> is disabled.
        </span>
      ),
    },
    {
      name: 'required',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Item</Code> choice is required in a form.
        </span>
      ),
    },
    {
      name: 'readOnly',
      type: 'boolean',
      description: (
        <span>
          Whether or not this <Code>Item</Code> is read-only and cannot be
          toggled by the user.
        </span>
      ),
    },
  ]}
/>

#### Indicator

Renders when the `RadioGroupItem` is the currently selected on. You can style this element directly, or you can use it as a wrapper to put an icon into, or both.

<PropsTable
  data={[
    {
      name: 'forceMount',
      type: 'boolean',
      description:
        'Used to force mounting when more control is needed. Useful when controlling animation with React animation libraries.',
    },
  ]}
/>
