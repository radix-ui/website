---
title: Separator
name: separator
version: 0.0.1
description: A primitive useful for separating things.
aria: https://www.w3.org/TR/wai-aria-1.2/#separator
features:
  - Supports horizontal and vertical orientations.
---

## Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-separator
```

## Anatomy

Import the components and piece the parts together.

```jsx
import { Separator } from '@radix-ui/react-separator';

export default () => <Separator />;
```

## Basic example

Create your styled separator component from the primitive part.

```jsx live manual
// import { styled } from 'path-to/stitches.config';
import * as SeparatorPrimitive from '@radix-ui/react-separator';

const Separator = styled(SeparatorPrimitive.Root, {
  backgroundColor: 'gainsboro',
  height: 1,
  margin: '20px 0',
});

export default () => (
  <>
    Content
    <Separator />
    Content
  </>
);
```

<Note>

**Note:** This example uses [Stitches](https://stitches.dev) but any styling solution is [compatible](../overview/styling).

</Note>

## API Reference

### Separator

`Separator` is the root component.

<PropsTable
  data={[
    {
      name: 'orientation',
      type: '"horizontal" | "vertical"',
      typeSimple: 'enum',
      default: '"horizontal"',
      description: (
        <span>
          The orientation of the <Code>Separator</Code>.
        </span>
      ),
    },
    {
      name: 'decorative',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Separator</Code> is purely visual
          and carries no semantic meaining. Setting this to{' '}
          <Code>true</Code> will ensure the separator is not present
          in the accessibility tree.
        </span>
      ),
    },
  ]}
/>

## Accessibility

Adheres to the [`separator` role requirements](https://www.w3.org/TR/wai-aria-1.2/#separator).
