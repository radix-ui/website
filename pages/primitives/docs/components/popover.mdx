---
title: Popover
name: popover
version: 0.0.1-13
description: An accessible Popover component that displays rich content as a result of clicking on a target.
features:
  - Can be controlled or uncontrolled.
  - Customise side, alignment, offsets, collision handling.
  - Optionally render a pointing arrow.
  - Focus is fully managed and customisable.
  - Dismissing and layering behavior is highly customisable.
---

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-popover
```

Then import the component in your component file.

```jsx
// Popover.tsx
import * as PopoverPrimitive from '@radix-ui/react-popover';
```

### Basic example

```jsx live manual
// Popover.tsx
// import * as PopoverPrimitive from '@radix-ui/react-popover';
// import { Button } from '@radix-ui/ds-button'

const PopoverContent = styled(PopoverPrimitive.Content, {
  borderRadius: 3,
  padding: '20px',
  fontSize: 14,
  backgroundColor: 'gainsboro',
  color: 'black',
});

render(
  <PopoverPrimitive.Root>
    <PopoverPrimitive.Trigger as={Button}>Trigger</PopoverPrimitive.Trigger>
    <PopoverPrimitive.Popper>
      <PopoverContent>
        <h3>Popover content</h3>
        <p>Are you sure you wanna do this?</p>
        <PopoverPrimitive.Close as={Button}>Yes</PopoverPrimitive.Close>
      </PopoverContent>
      <TooltipPrimitive.Arrow style={{ fill: 'gainsboro' }} />
    </PopoverPrimitive.Popper>
  </PopoverPrimitive.Root>
);
```

### Accessibility

#### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Space', 'Enter'],
      description: (
        <ul>
          <li>
            When focus is on <Code>Trigger</Code>, opens <Code>Popper</Code>.
          </li>
          <li>
            When focus is on <Code>Close</Code>, closes <Code>Popper</Code> and
            moves focus to <Code>Trigger</Code>.
          </li>
        </ul>
      ),
    },
    {
      keys: ['Tab'],
      description: (
        <ul>
          <li>Moves focus to the next focusable element.</li>
          <li>
            When focus is on the last focusable element within{' '}
            <Code>Popper</Code>, moves focus to the first focusable element.
          </li>
        </ul>
      ),
    },
    {
      keys: ['Shift + Tab'],
      description: (
        <ul>
          <li>Moves focus to the previous focusable element.</li>
          <li>
            When focus is on the first focusable element within{' '}
            <Code>Popper</Code>, moves focus to the last focusable element.
          </li>
        </ul>
      ),
    },
    {
      keys: ['Esc'],
      description: (
        <span>
          Closes <Code>Popper</Code> and moves focus to <Code>Trigger</Code>.
        </span>
      ),
    },
  ]}
/>

### API Reference

#### Popover

`Popover` is the root component.

<PropsTable
  data={[
    {
      name: 'defaultIsOpen',
      type: 'boolean',
      description: (
        <span>
          The open state of the <Code>Popover</Code> when it is initially
          rendered. Use <Code>defaultIsOpen</Code> if you do not need to control
          the state of an <Code>Popover</Code>.
        </span>
      ),
    },
    {
      name: 'isOpen',
      type: 'boolean',
      description: (
        <span>
          The controlled open state of the <Code>Popover</Code>. Must be used in
          conjunction with <Code>onIsOpenChange</Code>.
        </span>
      ),
    },
    {
      name: 'onIsOpenChange',
      type: '(isOpen: boolean) => void',
      typeSimple: 'function',
      description: (
        <span>
          The callback that fires when the state of the <Code>Popover</Code>{' '}
          changes.
        </span>
      ),
    },
  ]}
/>

#### PopoverTrigger

`PopoverTrigger` is the button that triggers the `Popover`. It is also used as the anchor the `PopoverPopper` is positioned against.

#### PopoverPopper

The component that pops out in an open `Popover`.

<PropsTable
  data={[
    {
      name: 'trapFocus',
      type: 'boolean',
      default: 'true',
      description: (
        <span>
          Whether or not focus should be trapped within the{' '}
          <Code>PopoverPopper</Code>.
        </span>
      ),
    },
    {
      name: 'onOpenAutoFocus',
      type: '(event: Event) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when auto-focusing on open. It can be prevented
          by calling <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'onCloseAutoFocus',
      type: '(event: Event) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when auto-focusing on close. It can be prevented
          by calling <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'disableOutsidePointerEvents',
      type: 'boolean',
      default: 'false',
      description: (
        <span>
          When <Code>true</Code>, hover/focus/click interactions will be
          disabled on elements outside the <Code>Popper</Code>. Users will need
          to click twice on outside elements to interact with them: Once to
          close the <Code>Popover</Code>, and again to trigger the element.
        </span>
      ),
    },
    {
      name: 'onEscapeKeyDown',
      type: '(event: KeyboardEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when the escape key is down. It can be prevented
          by calling <Code>event.preventDefault</Code>
        </span>
      ),
    },
    {
      name: 'onPointerDownOutside',
      type: '(event: MouseEvent | TouchEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when the a pointer event happens outside of the{' '}
          <Code>Popper</Code>. It can be prevented by calling{' '}
          <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'onFocusOutside',
      type: '(event: React.FocusEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when the focus moves outside of the{' '}
          <Code>Popper</Code>. It can be prevented by calling{' '}
          <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'onInteractOutside',
      type: '(event: React.FocusEvent | MouseEvent | TouchEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when an interaction happens outside the
          <Code>Popper</Code>. Specifically, when a pointer event happens outside
          of the `ContextMenuPopper` or focus moves outside of it. It can be prevented
          by calling <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'disableOutsideScroll',
      type: 'boolean',
      default: 'false',
      description: (
        <span>
          Whether scrolling outside the <Code>Popper</Code> should be prevented.
        </span>
      ),
    },
    {
      name: 'shouldPortal',
      type: 'boolean',
      default: 'true',
      description: (
        <span>
          Whether or not the <Code>Popper</Code> should render in a
          <Code>Portal</Code>.
        </span>
      ),
    },
    {
      name: 'forceMount',
      type: 'boolean',
      description: (
        <span>
          Used to force mounting when more control is needed. Useful when
          controlling animation with React animation libraries.
        </span>
      ),
    },
    {
      name: 'anchorRef',
      type: 'React.RefObject<MeasurableElement>',
      typeSimple: 'ref',
      description: (
        <span>
          An optional ref to an element to be used as the anchor for the{' '}
          <Code>Popper</Code>. By default, it will use <Code>Trigger</Code> as
          anchor element.
        </span>
      ),
    },
    {
      name: 'side',
      type: '"top" | "right" | "bottom" | "left"',
      typeSimple: 'enum',
      default: '"bottom"',
      description: (
        <span>
          The desired side to place the <Code>Popper</Code> against{' '}
          <Code>Trigger</Code>. May switch in case of collisions.
        </span>
      ),
    },
    {
      name: 'sideOffset',
      type: 'number',
      default: '0',
      description: (
        <span>
          The side offset for the <Code>Popper</Code>.
        </span>
      ),
    },
    {
      name: 'align',
      type: '"start" | "center" | "end"',
      typeSimple: 'enum',
      default: '"center"',
      description: (
        <span>
          How to align the <Code>Popper</Code> against <Code>Trigger</Code>. May
          switch in case of collisions.
        </span>
      ),
    },
    {
      name: 'alignOffset',
      type: 'number',
      default: '0',
      description: (
        <span>
          The align offset for the <Code>Popper</Code>
        </span>
      ),
    },
    {
      name: 'shouldAvoidCollisions',
      type: 'boolean',
      default: 'true',
      description: (
        <span>
          Whether or not collisions with window edges should be avoided. When
          turned on, this will result in the <Code>Popper</Code> flipping its
          side and alignment accordingly.
        </span>
      ),
    },
    {
      name: 'collisionTolerance',
      type: 'boolean',
      default: '0',
      description: (
        <span>
          The tolerance within which the <Code>Popper</Code> is to be checked
          against window edges.
        </span>
      ),
    },
  ]}
/>

#### PopoverArrow

The optional arrow element to render for the `Popover`. This can be used to help visually link the `PopoverTrigger` with the `PopoverPopper`. Must live inside `PopoverPopper`.

<PropsTable
  data={[
    {
      name: 'width',
      type: 'number',
      default: 10,
      description: (
        <span>
          The width of the <Code>Arrow</Code>.
        </span>
      ),
    },
    {
      name: 'height',
      type: 'number',
      default: 5,
      description: (
        <span>
          The height of the <Code>Arrow</Code>.
        </span>
      ),
    },
    {
      name: 'offset',
      type: 'number',
      default: 0,
      description: (
        <span>
          The offset of the `<Code>Arrow</Code> along the edge of{' '}
          <Code>Popper</Code>.
        </span>
      ),
    },
  ]}
/>

#### PopoverClose

The button that closes an open `Popover`.
