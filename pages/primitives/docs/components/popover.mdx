---
title: Popover
name: popover
version: 0.0.1-13
description: Displays rich content in a portal, triggered by a button.
features:
  - Can be controlled or uncontrolled.
  - Customise side, alignment, offsets, collision handling.
  - Optionally render a pointing arrow.
  - Focus is fully managed and customisable.
  - Dismissing and layering behavior is highly customisable.
---

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-popover
```

### Anatomy

Import the components and piece the parts together.

```jsx
import {
  Popover,
  Trigger,
  Content,
  Close,
  Arrow,
} from '@radix-ui/react-popover';

export default () => (
  <Popover>
    <Trigger />
    <Content>
      <Close />
      <Arrow />
    </Content>
  </Popover>
);
```

### Basic example

Create your styled popover component from the primitive parts.

**Note:** This example uses [Stitches](https://stitches.dev) but any styling solution is [compatible](../overview/styling).

```jsx live manual
// import { styled } from 'path-to/stitches.config';
import * as PopoverPrimitive from '@radix-ui/react-popover';

const Content = styled(PopoverPrimitive.Content, {
  borderRadius: 3,
  padding: '20px',
  fontSize: 14,
  backgroundColor: 'gainsboro',
  color: 'black',
});

export default () => (
  <PopoverPrimitive.Root>
    <PopoverPrimitive.Trigger>Trigger</PopoverPrimitive.Trigger>
    <Content>
      <h3>Popover content</h3>
      <p>Are you sure you wanna do this?</p>
      <PopoverPrimitive.Close>Yes</PopoverPrimitive.Close>
      <TooltipPrimitive.Arrow style={{ fill: 'gainsboro' }} />
    </Content>
  </PopoverPrimitive.Root>
);
```

### API Reference

#### Popover

`Popover` is the root component.

<PropsTable
  data={[
    {
      name: 'defaultOpen',
      type: 'boolean',
      description: (
        <span>
          The open state of the <Code>Popover</Code> when it is
          initially rendered. Use <Code>defaultOpen</Code> if you do
          not need to control the state of an <Code>Popover</Code>.
        </span>
      ),
    },
    {
      name: 'open',
      type: 'boolean',
      description: (
        <span>
          The controlled open state of the <Code>Popover</Code>. Must
          be used in conjunction with <Code>onOpenChange</Code>.
        </span>
      ),
    },
    {
      name: 'onOpenChange',
      type: '(open: boolean) => void',
      typeSimple: 'function',
      description: (
        <span>
          The callback that fires when the state of the{' '}
          <Code>Popover</Code> changes.
        </span>
      ),
    },
  ]}
/>

#### Trigger

`PopoverTrigger` is the button that triggers the `Popover`. It is also used as the anchor the `PopoverContent` is positioned against.

#### Content

The component that pops out in an open `Popover`.

<PropsTable
  data={[
    {
      name: 'trapFocus',
      type: 'boolean',
      default: 'true',
      description: (
        <span>
          Whether or not focus should be trapped within the{' '}
          <Code>PopoverContent</Code>.
        </span>
      ),
    },
    {
      name: 'onOpenAutoFocus',
      type: '(event: Event) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when auto-focusing on open. It can be
          prevented by calling <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'onCloseAutoFocus',
      type: '(event: Event) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when auto-focusing on close. It can be
          prevented by calling <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'disableOutsidePointerEvents',
      type: 'boolean',
      default: 'false',
      description: (
        <span>
          When <Code>true</Code>, hover/focus/click interactions will
          be disabled on elements outside the <Code>Content</Code>.
          Users will need to click twice on outside elements to
          interact with them: Once to close the <Code>Popover</Code>,
          and again to trigger the element.
        </span>
      ),
    },
    {
      name: 'onEscapeKeyDown',
      type: '(event: KeyboardEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when the escape key is down. It can be
          prevented by calling <Code>event.preventDefault</Code>
        </span>
      ),
    },
    {
      name: 'onPointerDownOutside',
      type: '(event: MouseEvent | TouchEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when the a pointer event happens
          outside of the <Code>Content</Code>. It can be prevented by
          calling <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'onFocusOutside',
      type: '(event: React.FocusEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when the focus moves outside of the{' '}
          <Code>Content</Code>. It can be prevented by calling{' '}
          <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'onInteractOutside',
      type:
        '(event: React.FocusEvent | MouseEvent | TouchEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when an interaction happens outside the
          <Code>Content</Code>. Specifically, when a pointer event happens
          outside of the `ContextMenuContent` or focus moves outside of
          it. It can be prevented by calling <Code>
            event.preventDefault
          </Code>.
        </span>
      ),
    },
    {
      name: 'disableOutsideScroll',
      type: 'boolean',
      default: 'false',
      description: (
        <span>
          Whether scrolling outside the <Code>Content</Code> should be
          prevented.
        </span>
      ),
    },
    {
      name: 'portalled',
      type: 'boolean',
      default: 'true',
      description: (
        <span>
          Whether or not the <Code>Content</Code> should render in a
          <Code>Portal</Code>.
        </span>
      ),
    },
    {
      name: 'forceMount',
      type: 'boolean',
      description: (
        <span>
          Used to force mounting when more control is needed. Useful
          when controlling animation with React animation libraries.
        </span>
      ),
    },
    {
      name: 'anchorRef',
      type: 'React.RefObject<MeasurableElement>',
      typeSimple: 'ref',
      description: (
        <span>
          An optional ref to an element to be used as the anchor for
          the <Code>Content</Code>. By default, it will use{' '}
          <Code>Trigger</Code> as anchor element.
        </span>
      ),
    },
    {
      name: 'side',
      type: '"top" | "right" | "bottom" | "left"',
      typeSimple: 'enum',
      default: '"bottom"',
      description: (
        <span>
          The desired side to place the <Code>Content</Code> against{' '}
          <Code>Trigger</Code>. May switch in case of collisions.
        </span>
      ),
    },
    {
      name: 'sideOffset',
      type: 'number',
      default: '0',
      description: (
        <span>
          The side offset for the <Code>Content</Code>.
        </span>
      ),
    },
    {
      name: 'align',
      type: '"start" | "center" | "end"',
      typeSimple: 'enum',
      default: '"center"',
      description: (
        <span>
          How to align the <Code>Content</Code> against{' '}
          <Code>Trigger</Code>. May switch in case of collisions.
        </span>
      ),
    },
    {
      name: 'alignOffset',
      type: 'number',
      default: '0',
      description: (
        <span>
          The align offset for the <Code>Content</Code>
        </span>
      ),
    },
    {
      name: 'avoidCollisions',
      type: 'boolean',
      default: 'true',
      description: (
        <span>
          Whether or not collisions with window edges should be
          avoided. When turned on, this will result in the{' '}
          <Code>Content</Code> flipping its side and alignment
          accordingly.
        </span>
      ),
    },
    {
      name: 'collisionTolerance',
      type: 'boolean',
      default: '0',
      description: (
        <span>
          The tolerance within which the <Code>Content</Code> is to be
          checked against window edges.
        </span>
      ),
    },
  ]}
/>

#### Arrow

The optional arrow element to render for the `Popover`. This can be used to help visually link the `PopoverTrigger` with the `PopoverContent`. Must live inside `PopoverContent`.

<PropsTable
  data={[
    {
      name: 'width',
      type: 'number',
      default: 10,
      description: (
        <span>
          The width of the <Code>Arrow</Code>.
        </span>
      ),
    },
    {
      name: 'height',
      type: 'number',
      default: 5,
      description: (
        <span>
          The height of the <Code>Arrow</Code>.
        </span>
      ),
    },
    {
      name: 'offset',
      type: 'number',
      default: 0,
      description: (
        <span>
          The offset of the `<Code>Arrow</Code> along the edge of{' '}
          <Code>Content</Code>.
        </span>
      ),
    },
  ]}
/>

#### Close

The button that closes an open `Popover`.

### Accessibility

#### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Space'],
      description: (
        <span>
          Opens/closes <Code>Content</Code>.
        </span>
      ),
    },
    {
      keys: ['Enter'],
      description: (
        <span>
          Opens/closes <Code>Content</Code>.
        </span>
      ),
    },
    {
      keys: ['Tab'],
      description: (
        <span>Moves focus to the next focusable element.</span>
      ),
    },
    {
      keys: ['Shift + Tab'],
      description: (
        <span>Moves focus to the previous focusable element.</span>
      ),
    },
    {
      keys: ['Esc'],
      description: (
        <span>
          Closes <Code>Content</Code> and moves focus to{' '}
          <Code>Trigger</Code>.
        </span>
      ),
    },
  ]}
/>
