---
title: Collapsible
name: collapsible
version: 0.0.1-13
description: Useful to group information into collapsible items.
features:
  - Full keyboard navigation.
  - Can be controlled or uncontrolled.
---

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-collapsible
```

Then import the component in your component file.

```jsx
// Avatar.tsx
import * as CollapsiblePrimitive from '@radix-ui/react-collapsible';
```

### Basic example

```jsx live manual
// Collapsible.tsx
// import * as CollapsiblePrimitive from '@radix-ui/react-collapsible';
// import { Button } from '@radix-ui/ds-button';

render(
  <CollapsiblePrimitive.Root>
    <CollapsiblePrimitive.CollapsibleButton as={Button}>
      Button
    </CollapsiblePrimitive.CollapsibleButton>
    <CollapsiblePrimitive.CollapsibleContent>
      Content 1
    </CollapsiblePrimitive.CollapsibleContent>
  </CollapsiblePrimitive.Root>
);
```

#### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Space', 'Enter'],
      description: (
        <ul>
          <li>
            When focus is on <Code>Button</Code> of a collapsed{' '}
            <Code>Collapsible</Code>, expands <Code>Content</Code>.
          </li>
          <li>
            When focus is on <Code>Button</Code> of an expanded{' '}
            <Code>Collapsible</Code>, collapses <Code>Content</Code>.
          </li>
        </ul>
      ),
    },
  ]}
/>

### API Reference

#### Collapsible

`Collapsible` is the root component.

<PropsTable
  data={[
    {
      name: 'defaultIsOpen',
      type: 'boolean',
      description: (
        <span>
          The open state of the <Code>Collapsible</Code> when it is initially
          rendered. Use <Code>defaultIsOpen</Code> if you do not need to control
          the state of an <Code>Collapsible</Code>.
        </span>
      ),
    },
    {
      name: 'isOpen',
      type: 'boolean',
      description: (
        <span>
          The controlled open state of the <Code>Collapsible</Code>. Must be
          used in conjunction with <Code>onToggle</Code>.
        </span>
      ),
    },
    {
      name: 'onToggle',
      type: '(isOpen: boolean) => void',
      typeSimple: 'function',
      description: (
        <span>
          The callback that fires when the open state of the{' '}
          <Code>Collapsible</Code> changes.
        </span>
      ),
    },
    {
      name: 'disabled',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Collapsible</Code> is disabled.
        </span>
      ),
    },
  ]}
/>

#### CollapsibleButton

The button that triggers the `Collapsible`.

#### CollapsibleContent

The component that contains the collapsible content.
