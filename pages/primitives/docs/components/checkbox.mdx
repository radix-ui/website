---
title: Checkbox
name: checkbox
version: 0.0.1-13
description: A control that allows the user to toggle between checked and not checked.
aria: https://www.w3.org/TR/wai-aria-practices-1.2/#checkbox
features:
  - Supports indeterminate state.
  - Full keyboard navigation.
  - Can be controlled or uncontrolled.
---

import { CheckboxHero } from '../../../../components/Hero/CheckboxHero';

<HeroSlot>
  <CheckboxHero />
</HeroSlot>

## Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-checkbox
```

## Anatomy

Import the components and piece the parts together.

```jsx
import { Root, Indicator } from '@radix-ui/react-checkbox';

export default () => (
  <Root>
    <Indicator />
  </Root>
);
```

## Basic example

Create your styled checkbox component from the primitive parts.

```jsx live manual
// import { styled } from 'path-to/stitches.config';
import * as CheckboxPrimitive from '@radix-ui/react-checkbox';
import { CheckIcon } from '@modulz/radix-icons';

const Checkbox = styled(CheckboxPrimitive.Root, {
  appearance: 'none',
  backgroundColor: 'transparent',
  border: 'none',
  padding: 0,
  boxShadow: 'inset 0 0 0 1px gainsboro',
  overflow: 'hidden',
  width: 15,
  height: 15,
  borderRadius: 2,

  '&:focus': {
    outline: 'none',
    boxShadow: 'inset 0 0 0 1px dodgerblue, 0 0 0 1px dodgerblue',
  },
});

const Indicator = styled(CheckboxPrimitive.Indicator, {
  alignItems: 'center',
  display: 'flex',
  height: '100%',
  justifyContent: 'center',
  width: '100%',
});

export default () => (
  <Checkbox defaultChecked>
    <Indicator>
      <CheckIcon />
    </Indicator>
  </Checkbox>
);
```

<Note>

**Note:** This example uses [Stitches](https://stitches.dev) but any styling solution is [compatible](../overview/styling).

</Note>

## API Reference

### Checkbox

`Checkbox` is the root component.

<PropsTable
  data={[
    {
      name: 'defaultChecked',
      type: 'boolean',
      description: (
        <span>
          The checked state of the <Code>Checkbox</Code> when it is
          initially rendered. Use <Code>defaultChecked</Code> if you
          do not need to control the state of the{' '}
          <Code>Checkbox</Code>.
        </span>
      ),
    },
    {
      name: 'checked',
      type: 'boolean',
      description: (
        <span>
          The controlled checked state of the <Code>Checkbox</Code>.
          Must be used in conjunction with{' '}
          <Code>onCheckedChange</Code>.
        </span>
      ),
    },
    {
      name: 'onCheckedChange',
      type: '(event: React.ChangeEvent<HTMLInputElement>) => void',
      typeSimple: 'function',
      description: (
        <span>
          The callback that fires when the checked state of the{' '}
          <Code>Checkbox</Code> changes.
        </span>
      ),
    },
    {
      name: 'disabled',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Checkbox</Code> is disabled.
        </span>
      ),
    },
    {
      name: 'required',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Checkbox</Code> is required in a
          form.
        </span>
      ),
    },
    {
      name: 'readOnly',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Checkbox</Code> is read-only and
          cannot be toggled by the user.
        </span>
      ),
    },
    {
      name: 'value',
      type: 'string',
      default: 'on',
      description: (
        <span>
          The value behind the <Code>Checkbox</Code>. Useful in forms.
        </span>
      ),
    },
  ]}
/>

### Indicator

Renders when the `Checkbox` is in a checked or indeterminate state. You can style this element directly, or you can use it as a wrapper to put an icon into, or both.

<PropsTable
  data={[
    {
      name: 'forceMount',
      type: 'boolean',
      description:
        'Used to force mounting when more control is needed. Useful when controlling animation with React animation libraries.',
    },
  ]}
/>

## Accessibility

Adheres to the [tri-state Checkbox WAI-ARIA design pattern](https://www.w3.org/TR/wai-aria-practices-1.2/#checkbox).

### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Space'],
      description: (
        <span>
          Checks/unchecks <Code>Checkbox</Code>.
        </span>
      ),
    },
    {
      keys: ['Enter'],
      description: (
        <span>
          Checks/unchecks <Code>Checkbox</Code>.
        </span>
      ),
    },
  ]}
/>
