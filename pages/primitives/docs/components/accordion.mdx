---
title: Accordion
name: accordion
version: 0.0.1-13
description: A vertically stacked set of interactive headings that each reveal an associated section of content.
aria: https://www.w3.org/TR/wai-aria-practices-1.1/#accordion
features:
  - Full keyboard navigation.
  - Expands one panel at a time.
  - Can be controlled or uncontrolled.
---

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-accordion
```

Then import the component in your component file.

```jsx
// Accordion.tsx
import * as AccordionPrimitive from '@radix-ui/react-accordion';
```

### Basic example

Create your styled accordion component from the accordion primitives.

```jsx live manual
// Accordion.tsx
// import * as AccordionPrimitive from '@radix-ui/react-accordion';

const AccordionItem = styled(AccordionPrimitive.Item, {
  borderTop: '1px solid $gray500',

  '&:last-of-type': {
    borderBottom: '1px solid $gray500',
  },
});

const AccordionHeader = styled(AccordionPrimitive.Header, {
  margin: 0,
  display: 'flex',
});

const AccordionButton = styled(AccordionPrimitive.Button, {
  backgroundColor: 'transparent',
  border: 'none',
  padding: 10,
  flex: 1,
});

const AccordionPanel = styled(AccordionPrimitive.Panel, {
  padding: 10,
});

render(
  <AccordionPrimitive.Root>
    <AccordionItem value="item-1">
      <AccordionHeader>
        <AccordionButton>Item 1</AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 1.</div>
      </AccordionPanel>
    </AccordionItem>

    <AccordionItem value="item-2">
      <AccordionHeader>
        <AccordionButton>Item 2</AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 2.</div>
      </AccordionPanel>
    </AccordionItem>

    <AccordionItem value="item-3">
      <AccordionHeader>
        <AccordionButton>Item 3</AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 3.</div>
      </AccordionPanel>
    </AccordionItem>
  </AccordionPrimitive.Root>
);
```

### Examples

#### With directional Icon

```jsx live manual compact
// Accordion.tsx
// import * as AccordionPrimitive from '@radix-ui/react-accordion';
// import { ChevronDownIcon } from '@modulz/radix-icons';

const AccordionItem = styled(AccordionPrimitive.Item, {
  borderTop: '1px solid $gray500',

  '&:last-of-type': {
    borderBottom: '1px solid $gray500',
  },
});

const AccordionHeader = styled(AccordionPrimitive.Header, {
  margin: 0,
  display: 'flex',
});

const AccordionButton = styled(AccordionPrimitive.Button, {
  backgroundColor: 'transparent',
  border: 'none',
  padding: 10,
  flex: 1,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
});

const AccordionPanel = styled(AccordionPrimitive.Panel, {
  padding: 10,
});

const AccordionChevron = styled(ChevronDownIcon, {
  transition: 'transform 300ms',

  '[data-state=open] &': {
    transform: 'rotate(180deg)',
  },
});

render(
  <AccordionPrimitive.Root>
    <AccordionItem value="item-1">
      <AccordionHeader>
        <AccordionButton>
          Item 1
          <AccordionChevron aria-hidden />
        </AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 1.</div>
      </AccordionPanel>
    </AccordionItem>

    <AccordionItem value="item-2">
      <AccordionHeader>
        <AccordionButton>
          Item 2
          <AccordionChevron aria-hidden />
        </AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 2.</div>
      </AccordionPanel>
    </AccordionItem>

    <AccordionItem value="item-3">
      <AccordionHeader>
        <AccordionButton>
          Item 3
          <AccordionChevron aria-hidden />
        </AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 3.</div>
      </AccordionPanel>
    </AccordionItem>
  </AccordionPrimitive.Root>
);
```

#### Expanded by default

```jsx live manual compact
// Accordion.tsx
// import * as AccordionPrimitive from '@radix-ui/react-accordion';
// import { ChevronDownIcon } from '@modulz/radix-icons';

const AccordionItem = styled(AccordionPrimitive.Item, {
  borderTop: '1px solid $gray500',

  '&:last-of-type': {
    borderBottom: '1px solid $gray500',
  },
});

const AccordionHeader = styled(AccordionPrimitive.Header, {
  margin: 0,
  display: 'flex',
});

const AccordionButton = styled(AccordionPrimitive.Button, {
  backgroundColor: 'transparent',
  border: 'none',
  padding: 10,
  flex: 1,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
});

const AccordionPanel = styled(AccordionPrimitive.Panel, {
  padding: 10,
});

const AccordionChevron = styled(ChevronDownIcon, {
  transition: 'transform 300ms',

  '[data-state=open] &': {
    transform: 'rotate(180deg)',
  },
});

render(
  <AccordionPrimitive.Root defaultValue="item-2">
    <AccordionItem value="item-1">
      <AccordionHeader>
        <AccordionButton>
          Item 1
          <AccordionChevron aria-hidden />
        </AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 1.</div>
      </AccordionPanel>
    </AccordionItem>

    <AccordionItem value="item-2">
      <AccordionHeader>
        <AccordionButton>
          Item 2
          <AccordionChevron aria-hidden />
        </AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 2.</div>
      </AccordionPanel>
    </AccordionItem>

    <AccordionItem value="item-3">
      <AccordionHeader>
        <AccordionButton>
          Item 3
          <AccordionChevron aria-hidden />
        </AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 3.</div>
      </AccordionPanel>
    </AccordionItem>
  </AccordionPrimitive.Root>
);
```

#### Card styles

```jsx live manual compact
// Accordion.tsx
// import * as AccordionPrimitive from '@radix-ui/react-accordion';
// import { ChevronDownIcon } from '@modulz/radix-icons';

const AccordionItem = styled(AccordionPrimitive.Item, {
  borderRadius: 6,
  boxShadow:
    'hsla(206,22%,7%,.15) 0px 5px 15px -5px, hsla(206,22%,7%,.1) 0px 5px 10px -15px',
  border: '1px solid rgba(255, 255, 255, 0.1)',
  marginBottom: 20,
  padding: 10,

  '&:last-of-type': {
    marginBottom: 0,
  },
});

const AccordionHeader = styled(AccordionPrimitive.Header, {
  margin: 0,
  display: 'flex',
});

const AccordionButton = styled(AccordionPrimitive.Button, {
  backgroundColor: 'transparent',
  border: 'none',
  padding: 10,
  flex: 1,
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  fontWeight: 500,
});

const AccordionPanel = styled(AccordionPrimitive.Panel, {
  padding: 10,
});

const AccordionChevron = styled(ChevronDownIcon, {
  transition: 'transform 300ms',

  '[data-state=open] &': {
    transform: 'rotate(180deg)',
  },
});

render(
  <AccordionPrimitive.Root>
    <AccordionItem value="item-1">
      <AccordionHeader>
        <AccordionButton>
          Item 1
          <AccordionChevron aria-hidden />
        </AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 1.</div>
      </AccordionPanel>
    </AccordionItem>

    <AccordionItem value="item-2">
      <AccordionHeader>
        <AccordionButton>
          Item 2
          <AccordionChevron aria-hidden />
        </AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 2.</div>
      </AccordionPanel>
    </AccordionItem>

    <AccordionItem value="item-3">
      <AccordionHeader>
        <AccordionButton>
          Item 3
          <AccordionChevron aria-hidden />
        </AccordionButton>
      </AccordionHeader>
      <AccordionPanel>
        <div>Here goes the content for the accordion item 3.</div>
      </AccordionPanel>
    </AccordionItem>
  </AccordionPrimitive.Root>
);
```

### Accessibility

Adheres to the [accordion WAI-ARIA design pattern](https://www.w3.org/TR/wai-aria-practices-1.1/#accordion).

#### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Space'],
      description: (
        <span>
          When focus is on a <Code>Button</Code> of a collapsed section, expands
          the section.
        </span>
      ),
    },
    {
      keys: ['Enter'],
      description: (
        <span>
          When focus is on a <Code>Button</Code> of a collapsed section, expands
          the section.
        </span>
      ),
    },
    {
      keys: ['Tab'],
      description: (
        <span>Moves focus to the next focusable element.</span>
      ),
    },
    {
      keys: ['Shift + Tab'],
      description: (
        <span>Moves focus to the previous focusable element.</span>
      ),
    },
    {
      keys: ['ArrowDown'],
      description: (
        <span>Moves focus to the next {' '}
          <Code>Button</Code>.
        </span>
      ),
    },
    {
      keys: ['ArrowUp'],
      description: (
        <span>Moves focus to the previous {' '}
          <Code>Button</Code>.
        </span>
      ),
    },
    {
      keys: ['Home'],
      description: (
        <span>
          When focus is on a <Code>Button</Code>, moves focus to the first{' '}
          <Code>Button</Code>.
        </span>
      ),
    },
    {
      keys: ['End'],
      description: (
        <span>
          When focus is on a <Code>Button</Code>, moves focus to the last{' '}
          <Code>Button</Code>.
        </span>
      ),
    },
  ]}
/>

### API Reference

#### Accordion

`Accordion` is the root component.

<PropsTable
  data={[
    {
      name: 'value',
      required: false,
      type: 'string',
      description: (
        <span>
          The controlled value of the <Code>Item</Code> whose panel is expanded.
          Must be used in conjunction with <Code>onValueChange</Code>.
        </span>
      ),
    },
    {
      name: 'defaultValue',
      required: false,
      type: 'string',
      description: (
        <span>
          The value of the <Code>Item</Code> whose panel is expanded when the{' '}
          <Code>Accordion</Code> is initially rendered. Use{' '}
          <Code>defaultValue</Code> if you do not need to control the state of
          an <Code>Accordion</Code>.
        </span>
      ),
    },
    {
      name: 'disabled',
      required: false,
      type: 'boolean',
      default: 'false',
      description: (
        <span>
          Whether or not an <Code>Accordion</Code> is disabled from user
          interaction.
        </span>
      ),
    },
    {
      name: 'onValueChange',
      required: false,
      type: '(value: string) => void',
      typeSimple: 'function',
      description: (
        <span>
          The callback that fires when the state of the <Code>Accordion</Code>{' '}
          changes.
        </span>
      ),
    },
  ]}
/>

#### Item

`AccordionItem` contains all of the parts of a collapsible section inside of an `Accordion`.

<PropsTable
  data={[
    {
      name: 'disabled',
      required: false,
      type: 'boolean',
      default: 'false',
      description: (
        <span>
          Whether or not an <Code>Item</Code> is disabled from user interaction.
        </span>
      ),
    },
    {
      name: 'value',
      required: true,
      type: 'string',
      description: (
        <span>
          A string value for the <Code>Item</Code>. All items within an{' '}
          <Code>Accordion</Code> should use a unique value.
        </span>
      ),
    },
  ]}
/>

#### Header

`AccordionHeader` wraps the `AccordionButton` and will be an `h2` by default. Use the `as` prop to update it to the appropriate heading level for your page.

#### Button

`AccordionButton` toggles the collapsed state of an `AccordionItem`. It should always be nested inside of an `AccordionHeader`.

#### Panel

`AccordionPanel` contains the collapsible content for an `AccordionItem`.
