---
title: Switch
name: switch
version: 0.0.1
description: A control that allows the user to toggle between checked and not checked.
features:
  - Full keyboard navigation.
  - Can be controlled or uncontrolled.
---

import { SwitchHero } from '../../../../components/Hero/SwitchHero';

<HeroSlot>
  <SwitchHero />
</HeroSlot>

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-switch
```

### Anatomy

Import the components and piece the parts together.

```jsx
import { Switch, Thumb } from '@radix-ui/react-switch';

export default () => (
  <Switch>
    <Thumb />
  </Switch>
);
```

### Basic example

```jsx live manual
// import { styled } from 'path-to/stitches.config';
import * as SwitchPrimitive from '@radix-ui/react-switch';

const Switch = styled(SwitchPrimitive.Root, {
  appearance: 'none',
  backgroundColor: 'transparent',
  border: 'none',
  padding: 0,
  width: 25,
  height: 15,
  backgroundColor: 'gainsboro',
  borderRadius: 25,
  position: 'relative',
  ':focus': {
    outline: 'none',
    boxShadow: '0 0 0 2px royalblue',
  },
  '&[data-state="checked"]': {
    backgroundColor: 'dodgerblue',
  },
});

const Thumb = styled(SwitchPrimitive.Thumb, {
  display: 'block',
  width: 13,
  height: 13,
  backgroundColor: 'white',
  borderRadius: '$round',
  boxShadow: 'rgba(0, 0, 0, 0.3) 0px 0px 2px',
  transition: 'transform 100ms',
  transform: 'translateX(1px)',
  willChange: 'transform',

  '&[data-state="checked"]': {
    transform: 'translateX(11px)',
  },
});

export default () => (
  <Switch>
    <Thumb />
  </Switch>
);
```

### API Reference

#### Switch

`Switch` is the root component.

<PropsTable
  data={[
    {
      name: 'defaultChecked',
      type: 'boolean',
      description: (
        <span>
          The checked state of the <Code>Switch</Code> when it is
          initially rendered. Use <Code>defaultChecked</Code> if you
          do not need to control the state of the <Code>Switch</Code>.
        </span>
      ),
    },
    {
      name: 'checked',
      type: 'boolean',
      description: (
        <span>
          The controlled checked state of the <Code>Switch</Code>.
          Must be used in conjunction with{' '}
          <Code>onCheckedChange</Code>.
        </span>
      ),
    },
    {
      name: 'onCheckedChange',
      type: '(event: React.ChangeEvent<HTMLInputElement>) => void',
      typeSimple: 'function',
      description: (
        <span>
          The callback that fires when the checked state of the{' '}
          <Code>Switch</Code> changes.
        </span>
      ),
    },
    {
      name: 'disabled',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Switch</Code> is disabled.
        </span>
      ),
    },
    {
      name: 'required',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Switch</Code> is required in a
          form.
        </span>
      ),
    },
    {
      name: 'readOnly',
      type: 'boolean',
      description: (
        <span>
          Whether or not the <Code>Switch</Code> is read-only and
          cannot be toggled by the user.
        </span>
      ),
    },
    {
      name: 'value',
      type: 'string',
      default: 'on',
      description: (
        <span>
          The value behind the <Code>Switch</Code>. Useful in forms.
        </span>
      ),
    },
  ]}
/>

#### Thumb

The thumb that is used to visually show whether the `Switch` is on or off.

### Accessibility

#### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Space'],
      description: "Toggles the component's state.",
    },
    {
      keys: ['Enter'],
      description: "Toggles the component's state.",
    },
  ]}
/>
