---
title: Switch
name: switch
version: 0.0.1
description: Useful for allowing users to toggle boolean options in a form.
---

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-switch
```

Then import the component in your component file.

```jsx
// Switch.tsx
import * as SwitchPrimitive from '@radix-ui/react-switch';
```

### Basic example

```jsx live manual
// Switch.tsx
// import * as SwitchPrimitive from '@radix-ui/react-switch';

const Switch = styled(SwitchPrimitive.Root, {
  appearance: 'none',
  backgroundColor: 'transparent',
  border: 'none',
  padding: 0,
  width: 25,
  height: 15,
  backgroundColor: 'gainsboro',
  borderRadius: 25,
  position: 'relative',
  ':focus': {
    outline: 'none',
    boxShadow: '0 0 0 2px royalblue',
  },
  '&[data-state="checked"]': {
    backgroundColor: 'dodgerblue',
  },
});

const Thumb = styled(SwitchPrimitive.Thumb, {
  display: 'block',
  width: 13,
  height: 13,
  backgroundColor: 'white',
  borderRadius: '$round',
  boxShadow: 'rgba(0, 0, 0, 0.3) 0px 0px 2px',
  transition: 'transform 100ms',
  transform: 'translateX(1px)',
  willChange: 'transform',

  '&[data-state="checked"]': {
    transform: 'translateX(11px)',
  },
});

render(
  <Switch>
    <Thumb />
  </Switch>
);
```

### Accessibility

#### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Space', 'Enter'],
      description: (
        <ul>
          <li>When off, changes to on state.</li>
          <li>When on, changes to off state.</li>
        </ul>
      ),
    },
  ]}
/>

### API Reference

#### Switch

`Switch` is the root component.

<PropsTable
  data={[
    {
      name: 'defaultChecked',
      type: 'boolean',
      description:
        'The checked state of the `Switch` when it is initially rendered. Use `defaultChecked` if you do not need to control the state of the `Switch`.',
    },
    {
      name: 'checked',
      type: 'boolean',
      description:
        'The controlled checked state of the `Switch`. Must be used in conjunction with `onCheckedChange`.',
    },
    {
      name: 'onCheckedChange',
      type: '(event: React.ChangeEvent<HTMLInputElement>) => void',
      typeSimple: 'function',
      description:
        'The callback that fires when the checked state of the `Switch` changes.',
    },
    {
      name: 'disabled',
      type: 'boolean',
      description: 'Whether the `Switch` is disabled or not.',
    },
    {
      name: 'required',
      type: 'boolean',
      description: 'Whether this choice is required in a form.',
    },
    {
      name: 'readOnly',
      type: 'boolean',
      description:
        'Whether this `Switch` is read-only and cannot be toggled by the user.',
    },
    {
      name: 'value',
      type: 'string',
      default: 'on',
      description: 'The value behind the `Switch`. Useful in forms.',
    },
  ]}
/>

#### SwitchThumb

`SwitchThumb` is the thumb that is used to visually show whether the `Switch` is on or off.
