---
title: Dialog
name: dialog
version: 0.0.1
description: A modal of content overlaid on top of a document's primary viewport.
features:
  - Focus is automatically trapped.
  - Works in an uncontrolled way by default.
  - Can also be controlled.
---

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-dialog
```

Then import the component in your component file.

```jsx
// Dialog.tsx
import * as DialogPrimitive from '@radix-ui/react-dialog';
```

### Basic example

Create your styled dialog component from the dialog primitives.

```jsx live manual
// Dialog.tsx
// import * as DialogPrimitive from '@radix-ui/react-dialog';
// import { Button } from '@radix-ui/ds-button'

const Overlay = styled(DialogPrimitive.DialogOverlay, {
  backgroundColor: 'rgba(0, 0, 0, .15)',
  position: 'fixed',
  top: 0,
  right: 0,
  bottom: 0,
  left: 0,
});

const Content = styled(DialogPrimitive.DialogContent, {
  position: 'fixed',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
  minWidth: 200,
  maxWidth: 'fit-content',
  maxHeight: '85vh',
  padding: 20,
  marginTop: '-5vh',
  backgroundColor: 'white',
  borderRadius: 6,

  '&:focus': {
    outline: 'none',
  },
});

render(
  <DialogPrimitive.Root>
    <DialogPrimitive.DialogTrigger as={Button}>
      Open
    </DialogPrimitive.DialogTrigger>
    <Overlay />
    <Content>
      <p>Dialog content</p>
      <DialogPrimitive.DialogClose as={Button}>
        Close
      </DialogPrimitive.DialogClose>
    </Content>
  </DialogPrimitive.Root>
);
```

### Examples

#### With CSS animations

```jsx live manual compact
// Dialog.tsx
// import * as DialogPrimitive from '@radix-ui/react-dialog';
// import { Button } from '@radix-ui/ds-button'

const fadeIn = css.keyframes({
  '0%': { opacity: 0 },
  '100%': { opacity: 1 },
});

const moveDown = css.keyframes({
  '0%': { transform: 'translate(-50%, calc(-50% - 20px))' },
  '100%': { transform: 'translate(-50%, -50%)' },
});

const Overlay = styled(DialogPrimitive.DialogOverlay, {
  backgroundColor: 'rgba(0, 0, 0, .15)',
  position: 'fixed',
  top: 0,
  right: 0,
  bottom: 0,
  left: 0,
  animation: `${fadeIn} 125ms linear`,
});

const Content = styled(DialogPrimitive.DialogContent, {
  position: 'fixed',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
  minWidth: 200,
  maxWidth: 'fit-content',
  maxHeight: '85vh',
  padding: 20,
  marginTop: '-5vh',
  backgroundColor: 'white',
  borderRadius: 6,
  animation: `${fadeIn} 125ms linear, ${moveDown} 325ms cubic-bezier(0.22, 1, 0.36, 1)`,

  '&:focus': {
    outline: 'none',
  },
});

render(
  <DialogPrimitive.Root>
    <DialogPrimitive.DialogTrigger as={Button}>
      Open
    </DialogPrimitive.DialogTrigger>
    <Overlay />
    <Content>
      <p>Dialog content</p>
      <DialogPrimitive.DialogClose as={Button}>
        Close
      </DialogPrimitive.DialogClose>
    </Content>
  </DialogPrimitive.Root>
);
```

### Accessibility

#### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Space', 'Enter'],
      description: (
        <ul>
          <li>
            When focus is on <Code>Trigger</Code>, opens <Code>Overlay</Code>{' '}
            and <Code>Content</Code>.
          </li>
          <li>
            When focus is on <Code>Close</Code>, closes <Code>Overlay</Code> and{' '}
            <Code>Content</Code> and moves focus to <Code>Trigger</Code>.
          </li>
        </ul>
      ),
    },
    {
      keys: ['Tab'],
      description: (
        <ul>
          <li>Moves focus to the next focusable element.</li>
          <li>
            When focus is on the last focusable element within{' '}
            <Code>Content</Code>, moves focus to the first focusable element.
          </li>
        </ul>
      ),
    },
    {
      keys: ['Shift + Tab'],
      description: (
        <ul>
          <li>Moves focus to the previous focusable element.</li>
          <li>
            When focus is on the first focusable element within{' '}
            <Code>Content</Code>, moves focus to the last focusable element.
          </li>
        </ul>
      ),
    },
    {
      keys: ['Esc'],
      description: (
        <span>
          Closes <Code>Overlay</Code> and <Code>Content</Code> and moves focus
          to <Code>Trigger</Code>.
        </span>
      ),
    },
  ]}
/>

### API Reference

#### Dialog

`Dialog` is the root component.

<PropsTable
  data={[
    {
      name: 'defaultOpen',
      type: 'boolean',
      description: (
        <span>
          The open state of the <Code>Dialog</Code> when it is initially
          rendered. Use <Code>defaultOpen</Code> if you do not need to control
          the state of an dialog.
        </span>
      ),
    },
    {
      name: 'open',
      type: 'boolean',
      description: (
        <span>
          The controlled open state of the <Code>Dialog</Code>. Must be used in
          conjunction with <pCode>onOpenChange</pCode>.
        </span>
      ),
    },
    {
      name: 'onOpenChange',
      type: '(open: boolean) => void',
      typeSimple: 'function',
      description: (
        <span>
          The callback that fires when the state of the <Code>Dialog</Code>{' '}
          changes.
        </span>
      ),
    },
    {
      name: 'id',
      type: 'string',
      description: (
        <span>
          A unique identifier for the <Code>Dialog</Code> component. The{' '}
          <Code>id</Code> is used to generate DOM id attributes for nested
          components. If no <Code>id</Code> prop is provided, a generated id
          will be used.
        </span>
      ),
    },
  ]}
/>

#### DialogTrigger

The button that triggers the `Dialog`.

#### DialogOverlay

The overlay that covers the inert portion of the view when a `Dialog` is open.

<PropsTable
  data={[
    {
      name: 'forceMount',
      type: 'boolean',
      description:
        'Used to force mounting when more control is needed. Useful when controlling animation with React animation libraries.',
    },
  ]}
/>

#### DialogContent

The component that contains content to be rendered in an open `Dialog`.

<PropsTable
  data={[
    {
      name: 'forceMount',
      type: 'boolean',
      description:
        'Used to force mounting when more control is needed. Useful when controlling animation with React animation libraries.',
    },
    {
      name: 'onOpenAutoFocus',
      type: '(event: Event) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when auto-focusing on open. It can be prevented
          by calling <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'onCloseAutoFocus',
      type: '(event: Event) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when auto-focusing on close. It can be prevented
          by calling <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'onEscapeKeyDown',
      type: '(event: KeyboardEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when the escape key is down. It can be prevented
          by calling <Code>event.preventDefault</Code>.
        </span>
      ),
    },
    {
      name: 'onPointerDownOutside',
      type: '(event: MouseEvent | TouchEvent) => void',
      typeSimple: 'function',
      description: (
        <span>
          Event handler called when the a pointer event happens outside of the{' '}
          <Code>AlertDialog</Code>. It can be prevented by calling{' '}
          <Code>event.preventDefault</Code>.
        </span>
      ),
    },
  ]}
/>

#### DialogClose

The button that closes an open `Dialog`.
