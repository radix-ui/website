---
title: Dialog
name: dialog
version: 0.0.1
features:
  - Focus is automatically trapped.
  - Works in an uncontrolled way by default.
  - Can also be controlled.
---

### Installation

Install the component from your command line.

```bash
npm install @radix-ui/react-dialog
```

Then import the component in your component file.

```jsx
// Dialog.tsx
import * as DialogPrimitive from '@interop-ui/react-dialog';
```

### Basic example

Create your styled dialog component from the dialog primitives.

```jsx live manual
// Dialog.tsx
// import * as DialogPrimitive from '@interop-ui/react-dialog';
// import { Button } from '@radix-ui/ds-button'

const Overlay = styled(DialogPrimitive.DialogOverlay, {
  backgroundColor: 'rgba(0, 0, 0, .15)',
  position: 'fixed',
  top: 0,
  right: 0,
  bottom: 0,
  left: 0,
});

const Content = styled(DialogPrimitive.DialogContent, {
  position: 'fixed',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
  minWidth: 200,
  maxWidth: 'fit-content',
  maxHeight: '85vh',
  padding: 20,
  marginTop: '-5vh',
  backgroundColor: 'white',
  borderRadius: 6,

  '&:focus': {
    outline: 'none',
  },
});

render(
  <DialogPrimitive.Dialog>
    <DialogPrimitive.DialogTrigger as={Button}>
      Open
    </DialogPrimitive.DialogTrigger>
    <Overlay />
    <Content>
      <p>Dialog content</p>
      <DialogPrimitive.DialogClose as={Button}>
        Close
      </DialogPrimitive.DialogClose>
    </Content>
  </DialogPrimitive.Dialog>
);
```

### Examples

#### With CSS animations

```jsx live manual compact
// Dialog.tsx
// import * as DialogPrimitive from '@interop-ui/react-dialog';
// import { Button } from '@radix-ui/ds-button'

const fadeIn = css.keyframes({
  '0%': { opacity: 0 },
  '100%': { opacity: 1 },
});

const moveDown = css.keyframes({
  '0%': { transform: 'translate(-50%, calc(-50% - 20px))' },
  '100%': { transform: 'translate(-50%, -50%)' },
});

const Overlay = styled(DialogPrimitive.DialogOverlay, {
  backgroundColor: 'rgba(0, 0, 0, .15)',
  position: 'fixed',
  top: 0,
  right: 0,
  bottom: 0,
  left: 0,
  animation: `${fadeIn} 125ms linear`,
});

const Content = styled(DialogPrimitive.DialogContent, {
  position: 'fixed',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
  minWidth: 200,
  maxWidth: 'fit-content',
  maxHeight: '85vh',
  padding: 20,
  marginTop: '-5vh',
  backgroundColor: 'white',
  borderRadius: 6,
  animation: `${fadeIn} 125ms linear, ${moveDown} 325ms cubic-bezier(0.22, 1, 0.36, 1)`,

  '&:focus': {
    outline: 'none',
  },
});

render(
  <DialogPrimitive.Dialog>
    <DialogPrimitive.DialogTrigger as={Button}>
      Open
    </DialogPrimitive.DialogTrigger>
    <Overlay />
    <Content>
      <p>Dialog content</p>
      <DialogPrimitive.DialogClose as={Button}>
        Close
      </DialogPrimitive.DialogClose>
    </Content>
  </DialogPrimitive.Dialog>
);
```

### Accessibility

#### Keyboard Interactions

<KeyboardTable
  data={[
    {
      keys: ['Esc'],
      description: <span>Closes the dialog</span>,
    },
  ]}
/>

### API Reference

#### Dialog

`Dialog` is the root component.

<PropsTable
  data={[
    {
      name: 'defaultIsOpen',
      type: 'boolean',
      description:
        'The open state of the `Dialog` when it is initially rendered. Use `defaultIsOpen` if you do not need to control the state of an `Dialog`.',
    },
    {
      name: 'isOpen',
      type: 'boolean',
      description:
        'The controlled open state of the `Dialog`. Must be used in conjunction with `onIsOpenChange`.',
    },
    {
      name: 'onIsOpenChange',
      type: '(isOpen: boolean) => void',
      typeSimple: 'function',
      description:
        'The callback that fires when the state of the `Dialog` changes.',
    },
    {
      name: 'id',
      type: 'string',
      description:
        'A unique identifier for the `Dialog` component. The `id` is used to generate DOM id attributes for nested components. If no `id` prop is provided, a generated id will be used.',
    },
  ]}
/>

#### DialogOverlay

`DialogOverlay` is the overlay that covers the inert portion of the view when a `Dialog` is open.

<PropsTable
  data={[
    {
      name: 'forceMount',
      type: 'boolean',
      description:
        'Used to force mounting when more control is needed. Useful when controlling animation with React animation libraries.',
    },
  ]}
/>

#### DialogContent

`DialogContent` is the component that contains content to be rendered in an open `Dialog`.

<PropsTable
  data={[
    {
      name: 'forceMount',
      type: 'boolean',
      description:
        'Used to force mounting when more control is needed. Useful when controlling animation with React animation libraries.',
    },
    {
      name: 'onOpenAutoFocus',
      type: '(event: Event) => void',
      typeSimple: 'function',
      description:
        'Event handler called when auto-focusing on open. It can be prevented by calling `event.preventDefault`.',
    },
    {
      name: 'onCloseAutoFocus',
      type: '(event: Event) => void',
      typeSimple: 'function',
      description:
        'Event handler called when auto-focusing on close. It can be prevented by calling `event.preventDefault`.',
    },
    {
      name: 'onEscapeKeyDown',
      type: '(event: KeyboardEvent) => void',
      typeSimple: 'function',
      description:
        'Event handler called when the escape key is down. It can be prevented by calling `event.preventDefault`',
    },
    {
      name: 'onPointerDownOutside',
      type: '(event: MouseEvent | TouchEvent) => void',
      typeSimple: 'function',
      description:
        'Event handler called when the a pointer event happens outside of the `Dialog`. It can be prevented by calling `event.preventDefault`.',
    },
  ]}
/>

#### DialogTrigger

`DialogTrigger` is the button that triggers the `Dialog`.

#### DialogClose

`DialogClose` is the button that closes an open `Dialog`.
