import { styled, Text } from '@modulz/design-system';

export const MarketingCaption = styled(Text, {
  fontWeight: 500,
  lineHeight: '20px',
  defaultVariants: {
    size: 3,
    variant: 'teal',
  },
});
