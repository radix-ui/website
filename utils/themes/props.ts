// Re-export each prop definition explicitly rather than using `export *`.
// A wildcard re-export produces a dynamic, non-extensible namespace proxy under
// Turbopack, which crashes when a consumer spreads/enumerates the namespace
// (e.g. `const { ...propDefs } = props`). Explicit named re-exports keep the
// namespace static so it can be safely enumerated.
export {
	accentColorPropDef,
	accentColors,
	alertDialogContentPropDefs,
	asChildPropDef,
	avatarPropDefs,
	badgePropDefs,
	blockquotePropDefs,
	boxPropDefs,
	breakpoints,
	buttonPropDefs,
	calloutRootPropDefs,
	cardPropDefs,
	checkboxCardsRootPropDefs,
	checkboxGroupRootPropDefs,
	checkboxPropDefs,
	codePropDefs,
	colorPropDef,
	containerPropDefs,
	contextMenuCheckboxItemPropDefs,
	contextMenuContentPropDefs,
	contextMenuItemPropDefs,
	contextMenuRadioItemPropDefs,
	dataListItemPropDefs,
	dataListLabelPropDefs,
	dataListRootPropDefs,
	dialogContentPropDefs,
	dropdownMenuCheckboxItemPropDefs,
	dropdownMenuContentPropDefs,
	dropdownMenuItemPropDefs,
	dropdownMenuRadioItemPropDefs,
	emPropDefs,
	flexPropDefs,
	gapPropDefs,
	grayColors,
	gridPropDefs,
	headingPropDefs,
	heightPropDefs,
	highContrastPropDef,
	hoverCardContentPropDefs,
	iconButtonPropDefs,
	insetPropDefs,
	kbdPropDefs,
	layoutPropDefs,
	leadingTrimPropDef,
	linkPropDefs,
	marginPropDefs,
	paddingPropDefs,
	popoverContentPropDefs,
	progressPropDefs,
	quotePropDefs,
	radii,
	radioCardsRootPropDefs,
	radioGroupRootPropDefs,
	radioPropDefs,
	radiusPropDef,
	scrollAreaPropDefs,
	sectionPropDefs,
	segmentedControlRootPropDefs,
	selectContentPropDefs,
	selectRootPropDefs,
	selectTriggerPropDefs,
	separatorPropDefs,
	skeletonPropDefs,
	sliderPropDefs,
	spinnerPropDefs,
	strongPropDefs,
	switchPropDefs,
	tabNavLinkPropDefs,
	tabNavRootPropDefs,
	tableCellPropDefs,
	tableRootPropDefs,
	tableRowPropDefs,
	tabsContentPropDefs,
	tabsListPropDefs,
	tabsRootPropDefs,
	textAlignPropDef,
	textAreaPropDefs,
	textFieldRootPropDefs,
	textFieldSlotPropDefs,
	textPropDefs,
	textWrapPropDef,
	themePropDefs,
	tooltipPropDefs,
	truncatePropDef,
	weightPropDef,
	widthPropDefs,
} from "@radix-ui/themes/dist/esm/props/index.js";
